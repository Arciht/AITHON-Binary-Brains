const button = document.querySelector('button');

button.addEventListener('click', () => {
    // Add your desired functionality here, e.g., redirect to a new page
    console.log('TRY NOW button clicked!');
});